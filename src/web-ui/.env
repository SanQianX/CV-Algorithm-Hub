# Vite Environment Variables for Vue Frontend
# Note: Variable names must start with VITE_
# For Docker deployment, use relative paths to go through nginx proxy

VITE_API_URL=http://localhost:8080
VITE_API_BASE_URL=/api/v1
